import { CourseNewLineDirective } from './course-new-line.directive';

describe('CourseNewLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseNewLineDirective();
    expect(directive).toBeTruthy();
  });
});
